package com.tests;

import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

import recipe.Receipedata;

public class TestHibernateConnect {

	/*
	 * public static void main(String[] args) { // TODO Auto-generated method stub
	 * 
	 * SessionFactory sessionFactory = new Configuration().configure()
	 * .buildSessionFactory(); Session session = sessionFactory.openSession();
	 * session.beginTransaction();
	 * 
	 * Receipedata receipe = new Receipedata(); receipe.setRecipe_ID("1");
	 * receipe.setRecipe_Name("1"); receipe.setRecipe_Category("1");
	 * receipe.setFood_Category("1"); receipe.setIngredients("1");
	 * receipe.setPreparation_Time(1); receipe.setCooking_Time(1);
	 * 
	 * session.save(receipe); session.getTransaction().commit(); session.close();
	 * 
	 * }
	 */

}
